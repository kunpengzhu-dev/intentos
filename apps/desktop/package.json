{
  "name": "@intentos/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "dev": "concurrently -k \"pnpm --filter @intentos/backend dev\" \"pnpm --filter @intentos/frontend dev\" \"wait-on tcp:3030 tcp:5173 && cross-env ELECTRON_RENDERER_URL=http://localhost:5173 INTENTOS_BACKEND_URL=http://localhost:3030 electron .\"",
    "start": "pnpm --filter @intentos/frontend build && electron .",
    "build": "pnpm --filter @intentos/frontend build",
    "test": "echo \"No desktop tests yet\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "electron": "^31.0.0",
    "wait-on": "^8.0.5"
  }
}
